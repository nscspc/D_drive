# isdisjoint( ) :- it checks whether the sets have an intersection or not . If the sets don't have common items, this method returns True, otherwise it returns False.

s1={1,2,3,4,5}
s2={5,4,5,7,8}
s3={10,90,40,50,60}

print(s1.isdisjoint(s2))
print(s2.isdisjoint(s3))
