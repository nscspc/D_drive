from two import display#name of file cannot be a number in this case
#import two#this is wrong as function name with the file name should also be specified here.
#import display#this is wrong as file(module) name should also be specified with function name.
print(display(499))
